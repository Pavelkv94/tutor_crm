/**
 * Глобальные настройки школы. Строка в БД ровно одна, поэтому сущность без id:
 * снаружи это просто набор значений, а не объект с идентичностью.
 */
export class SchoolSettingsEntity {
	/** Внутренний курс евро в сотых долях BYN: 500 = 1 € = 5.00 BYN. 0 — курс не задан. */
	eur_rate: number;

	/** null — строки настроек ещё нет и курс никогда не задавали. */
	updated_at: Date | null;
}
